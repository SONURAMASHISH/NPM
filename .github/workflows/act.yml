name: Python application

on:
  schedule:
    - cron: '46 10 * * *'
  workflow_dispatch:      

jobs:
  run-loop:
    runs-on: macos-latest  # Runs on a Windows Cloud OS

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.14"

    # Clean, optimized loop for Windows
    - name: Run Package Install/Uninstall Loop
      shell: powershell
      run: |
        for ($i = 1; $i -le 160000; $i++) {
            Write-Host "Iteration $i of 160000"
            
            # 1. Install the main package (this pulls in both npmai-agents and npmai)
            pip install --no-cache-dir npmai-agents==1.0.2 
            
            # 2. Uninstall both packages simultaneously in one clean command
            pip uninstall -y npmai-agents==1.0.2 npmai==0.1.9
            
            Write-Host "-------------------------------------"
        }
